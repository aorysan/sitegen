{
  "name": "sitegen-skill",
  "private": true,
  "type": "module",
  "description": "QC tooling untuk skill sitegen — hanya untuk screenshot verifikasi, BUKAN untuk output landing page (landing page = HTML murni).",
  "scripts": {
    "generate": "node scripts/generate.js",
    "qc": "node scripts/render.cjs"
  },
  "dependencies": {
    "node-vibrant": "^3.1.6",
    "pdf-parse": "^2.4.5",
    "puppeteer": "^25.3.0",
    "sharp": "^0.35.3"
  }
}
